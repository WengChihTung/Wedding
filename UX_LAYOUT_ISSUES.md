# 婚禮喜帖網站｜操作與 Layout 待處理清單

最後更新：2026-08-16  
狀態：第 1～12 項已完成程式修正，待瀏覽器與實機完整驗證；音樂播放決策本輪不變更

## 範圍

本文件只追蹤已確認需要處理的操作與響應式 Layout 問題。婚禮內容、資訊順序與功能擴充不在本輪範圍內。

## 1. 觸控板或快速滑動可能連跳多頁

### 現況

- 頁面轉場清理時間為 `920ms`，滾輪鎖定只有 `620ms`。
- Mac 觸控板的慣性滾動可能在鎖定解除後，再次觸發下一頁。
- 手機 `touchend` 換頁目前沒有共用的轉場鎖，快速連滑可能跳過頁面或打斷前一次動畫。

### 預期處理

- 滾輪、觸控與按鈕換頁共用同一個轉場狀態。
- 一次完整手勢最多換一頁。
- 轉場完成或明確進入可再次操作的時間點後，才解除鎖定。
- 需保留快速操作後仍可繼續換頁，不能因鎖定機制永久卡住。

### 實作狀態

- 所有換頁來源共用以 `performance.now()` 計算的到期時間鎖，頁面轉場鎖定 920ms；開信封期間鎖定 1650ms 並在銜接內容頁時重新鎖定。鎖定不再依賴布林值與解鎖計時器，因此即使分頁被節流也不會永久卡住。
- 觸控板的小幅 wheel 位移會累積至 24px 再換頁；每次成功換頁後冷卻 1040ms，避免同一段慣性連跳。
- 觸控手勢若開始於轉場期間會被忽略，按鈕與手機滑動也無法打斷前一次換頁。

## 2. 使用空白鍵操作按鈕時可能重複換頁

### 現況

- 全域 `keydown` 把 Space 視為下一頁。
- HTML 按鈕在聚焦時按 Space，也會由瀏覽器產生原生 `click`。
- 當焦點位於下一頁、影片播放或音樂按鈕時，可能同時觸發全域換頁與按鈕原生行為。

### 預期處理

- 全域鍵盤導覽應忽略按鈕、連結、影片控制項等互動元素。
- 對全域接管的方向鍵、Page Up、Page Down 與 Space 適當使用 `preventDefault()`。
- 鍵盤操作一次只能產生一個明確結果。

### 實作狀態

- 全域鍵盤導覽已忽略按鈕、連結、表單欄位、影音元素與可編輯內容。
- 接管方向鍵、Page Up、Page Down 與 Space 時會呼叫 `preventDefault()`，並忽略長按產生的 `repeat` 事件。
- 影片開啟時只有 Escape 由全域處理，其餘按鍵交還影片控制項。

## 3. 平板直向與窄桌面視窗的場地頁可能橫向裁切

### 現況

- 手機單欄版斷點為 `760px`。
- `761px` 以上的場地頁使用兩欄，左右欄最小寬度分別為 `320px` 與 `420px`，另有 `7vw` 間距與左右各 `8vw` padding。
- 約 `761px`～`947px` 的視窗無法容納此最小寬度；常見的 iPad 直向尺寸仍會套用桌面兩欄，文字或地圖可能被滿版容器裁掉。

### 預期處理

- 場地頁應在實際容納不下兩欄前提早切換為單欄，而不是只依手機寬度判斷。
- 驗證至少包含 `768×1024`、`810×1080`、`820×1180`、`834×1194` 與窄桌面視窗。
- 不可產生橫向捲動，也不可用 `overflow:hidden` 掩蓋被裁掉的內容。

### 實作狀態

- `761px～1040px` 改為單欄場地資訊與地圖；高度不超過 700px 的橫向平板／窄桌面改用可收縮的雙欄配置。
- 同步壓縮場地資訊間距與地圖高度，短手機不再隱藏婚禮日期。

## 4. Dress Code 在矮桌面與短手機可能被裁掉

### 現況

- Dress Code 內容只設定 `max-height`，不會自動縮小內部圖片、文字與裝飾。
- 頁面本身禁止捲動，超出可視高度的內容會直接被裁切。
- 現有 `max-height:650px` 補救規則只處理日期、場地與流程頁，沒有處理 Dress Code。
- `1366×768` 與 `1440×900` 桌面尺寸，以及較短的手機螢幕，都需要優先確認。

### 預期處理

- 依可用高度同步調整插畫高度、間距、標題與說明文字，而不是只限制外層高度。
- 保留所有穿搭說明，不以隱藏重要文字作為主要解法。
- 仍維持滿版換頁，不改成頁內捲動。

### 實作狀態

- 桌面高度 `900px` 以下會同步縮小插畫、標題、間距、文字與裝飾，女士／男士說明改成兩欄以節省垂直空間。
- 桌面高度 `600px` 以下及手機高度 `700px` 以下另有更緊湊級距，所有穿搭說明仍保留。

## 5. 固定控制項未處理裝置安全區

### 現況

- 頁面使用 `viewport-fit=cover`，內容可能延伸到瀏海、Dynamic Island 與 Home Indicator 區域。
- 左上頁碼、左下音樂控制、底部進度條及右側導覽目前使用固定像素位置，沒有加入 `env(safe-area-inset-top)`、`env(safe-area-inset-right)` 或 `env(safe-area-inset-bottom)`。

### 預期處理

- 固定控制項的位置需疊加對應的 safe-area inset。
- iPhone 直向、橫向及 Android 手勢導覽模式都需確認。
- 安全區調整不得造成桌面版控制項偏移或內容重疊。

### 實作狀態

- 左上頁碼、右側導覽、左下音樂控制、底部進度條與內容頁向下提示均已疊加對應的 `safe-area-inset-*`。
- `env()` 提供 `0px` fallback，桌面沒有安全區時維持原位置。

## 6. 內容頁碼與右側導覽頁碼不一致

### 現況

- 內容頁左上角使用 `01`～`10`，不把封面計入。
- 右側導覽把封面計入總數，開封後顯示 `02/11`，最後一頁顯示 `11/11`。
- 同一畫面會同時出現相差一頁的兩個頁碼。

### 預期處理

- 統一頁碼的計數邏輯與總頁數定義。
- 若封面不屬於內容頁，右側導覽應在開封後以 `01/10` 起算；若封面要計入，內容頁左上角也需使用同一套規則。

### 實作狀態

- 封面不計入內容頁總數；開封後右側導覽與左上頁碼統一為 `01/10`～`10/10`。
- 返回封面時隱藏右側導覽與進度條，再次開封後重新從 `01/10` 顯示。

## 7. 手機封面開啟提示不明顯

### 實作狀態

- 手機版「輕觸開啟喜帖」改用較深的文字色與雙層文字陰影。
- 下方動畫線由 1px 淡黃色改成 2px 灰玫瑰色，加入亮邊與柔和陰影；動畫節奏不變。

## 8. 手機向上滑開封未觸發背景音樂

### 實作狀態

- 觸控開始時只在封面預備音訊資源；確認為有效向上滑動後，在同一次 `touchend` 使用者手勢內直接要求播放。
- 加入播放 Promise 防重入，避免滑動流程與 `openInvitation()` 同時重複呼叫 `play()`。
- 音樂播放決策不變：第一次開啟喜帖仍會播放，影片開始時暫停，影片結束或關閉後再接續。

## 9. 第二個內容頁起的底部箭頭不可點擊

### 實作狀態

- 原本由 CSS pseudo-element 顯示、`pointer-events:none` 的裝飾箭頭已改為 48×48px 的實際按鈕。
- 第 3～10 頁均可點擊底部箭頭前往下一頁；第 2 頁保留既有「繼續閱讀」按鈕，第 11 頁維持無向下提示。
- 影片開啟時會隱藏影片頁底部箭頭，避免覆蓋播放器。

## 10. 桌面開封與換頁偶發卡住

### 實作狀態

- 導覽鎖改為到期時間判斷，避免解鎖 timer 被背景分頁節流後留下錯誤鎖定狀態。
- wheel 手勢改成累積位移，Mac 觸控板較輕的滑動也能可靠開啟封面；轉場期間的慣性尾端不會多翻一頁。
- `deltaMode` 改用標準數值判斷，不再依賴部分桌面環境可能未暴露的 `WheelEvent.DOM_DELTA_*` 全域常數；轉場期間只忽略 wheel，不把下一個手勢永久標記為已消耗。
- `openInvitation()` 與 `move()` 都回傳明確的成功狀態，並共用同一套鎖定判斷。

## 11. 人物頁文案與新娘照片裁切

### 實作狀態

- 移除新郎「往後的每一天……」與新娘「謝謝你走進我的生命……」兩段姓名下方文案，保留英文姓名與相框圖說。
- 新娘桌面照片改用 `object-fit:contain`，以完整人物構圖優先，不再用填滿相框的方式大量裁掉身體。

## 12. 標題與結尾文字孤字換行

### 實作狀態

- 活動流程標題設為單行，並在 761～1100px 降低字級與字距。
- Dress Code 男士說明將「西裝外套與長褲」設為不可拆分片段。
- 最後一頁兩行結尾文字改為各自的不可換行區塊，並以響應式字級確保窄手機與桌面欄寬內完整呈現。

## 音樂互動待決策：類似 Instagram 的預設無聲模式

> 本輪依使用者指示不修改音樂播放決策，以下內容保留作後續評估。

### 需求方向

希望避免賓客開啟喜帖時突然出聲，改為預設無聲，點擊音量控制後才播放音樂；如果實作可能造成音樂永久靜音或部分手機無法再播放，則不採用。

### 技術判斷

此互動可以可靠實作，但建議採用「預設暫停，點擊後播放」，不建議讓 MP3 在背景靜音播放後再解除靜音。

推薦流程：

1. 開啟信封時不呼叫 `bgm.play()`，音樂按鈕顯示為關閉狀態。
2. 賓客第一次點擊音樂按鈕時，在該次 `click` 事件內直接呼叫 `bgm.play()`。
3. 只有 `play()` 成功後才把按鈕切換為播放狀態；若 Promise 被瀏覽器拒絕，維持關閉狀態並允許再次點擊。
4. 後續點擊以 `pause()`／`play()` 切換，不依賴永久儲存的靜音旗標。
5. 不把靜音偏好寫入 `localStorage`；重新開啟網站仍維持預設無聲，避免形成使用者難以察覺的「永久靜音」。
6. 婚禮影片結束後，只有在影片播放前背景音樂原本正在播放時才恢復。

### 不建議的方案

不建議讓 `<audio>` 先以 muted 狀態自動播放，再等待使用者解除靜音，原因包括：

- 靜音期間仍可能下載、解碼並推進播放進度，對背景音樂沒有實質體驗價值。
- Safari 對有聲媒體採較嚴格的使用者手勢政策；若解除靜音不在明確點擊事件內，媒體可能被暫停或播放 Promise 被拒絕。
- Chrome 雖允許 muted autoplay，但不同瀏覽器的狀態處理不完全一致，會增加沒有必要的例外流程。
- 賓客稍後開啟聲音時，歌曲可能已經播放到中段；直接從暫停狀態開始更符合預期。

因此，此方案不會有「永久靜音」的技術風險；它本質上是由使用者明確按下音樂按鈕才開始播放。正式修改前仍需確認是否接受取消開封時自動播放。

### 參考資料

- [WebKit：iOS 媒體播放政策](https://webkit.org/blog/6784/new-video-policies-for-ios/)
- [WebKit：macOS 自動播放政策與建議](https://webkit.org/blog/7734/auto-play-policy-changes-for-macos/)
- [Chrome for Developers：Autoplay policy](https://developer.chrome.com/blog/autoplay/)
- [WHATWG HTML Standard：Media elements](https://html.spec.whatwg.org/multipage/media.html)

## 驗證原則

- 修正後仍需完整執行 `PROJECT_CONTEXT.md` 的既有測試清單。
- 操作問題需用滑鼠滾輪、Mac 觸控板、手機快速連滑與鍵盤分別測試。
- Layout 問題需同時驗證手機、平板直向、窄桌面、`1440×900`、`1920×1080` 與 `1920×1200`。
- 本文件中的項目在實際修正並通過測試前，不標記為完成。

## 2026-08-16 本輪驗證紀錄

- 已通過：JavaScript 語法、CSS 大括號結構、11 個場景／10 個內容頁計數、所有本機素材路徑與 MP3 128 kbps 完整解碼。
- 已以規則檢查確認 `768×1024`、`810×1080`、`820×1180`、`834×1194` 會進入場地頁單欄斷點；`1366×768`、`1440×900` 會進入 Dress Code 矮桌面級距。
- 尚待瀏覽器／實機：Mac 觸控板慣性、手機快速連滑、iPhone 安全區、Android 手勢導覽，以及上述 viewport 的實際文字與圖片裁切。
- 本機瀏覽器測試介面本輪沒有可用實例，因此第 1～6 項維持「已實作、待完整驗證」，不標記為完全完成。
- 新增回饋修正後再次通過：`script.js` 可由 JavaScript 執行環境解析、CSS 大括號成對、11 個 `data-page` 完整、8 個新增底部箭頭按鈕存在、本機引用素材路徑無缺漏、`git diff --check` 無空白錯誤。
- 本輪瀏覽器連線仍回報沒有可用實例，因此第 7～12 項也維持「已實作、待瀏覽器與真機回歸」。

## 2026-08-16 第二輪真機回饋

- 手機封面提示動畫線由 2px 調回 1px，保留深色與柔和陰影，因此更細但仍能與背景區分。
- 桌面 wheel handler 已移除 `WheelEvent.DOM_DELTA_*` 相依，累積門檻與手勢重置時間同步降低。
- 婚禮影片由 87 MB、H.264 High Profile Level 4.0 重新輸出為約 21 MB、960×540、H.264 Main Profile Level 3.1、AAC、fast-start 版本；37 秒影片大小減少約 75%。
- 播放器新增載入中、網路較慢與載入失敗狀態；失敗時保留播放器並提供重新載入，不再直接關閉而讓使用者無從判斷。
- 新影片已由 macOS AVFoundation 成功解碼並再次轉出 1 秒測試片段；仍需在實際 GitHub Pages 與 iPhone／Android 確認網路串流。

## 2026-08-16 第三輪真機回饋

- 影片改採 1280×720 H.264 Main／AAC fast-start，約 31 MB；較 540p 清楚，仍比原始 87 MB 減少約 64%。
- MacBook 兩指捲動不是不建議的操作；滿版喜帖仍保留 wheel 翻頁，並讓右側導覽、底部箭頭與鍵盤作為備援。
- wheel listener 改掛在 `document` 捕獲階段，並加入 `mousewheel` 相容事件；移除 `wheelGestureConsumed` 與解鎖 timer，改用位移累積、方向重置及到期時間冷卻。
