# Agent Instructions

在修改本專案前，先完整閱讀 `PROJECT_CONTEXT.md`。

重要規則：

- 所有未確認的修改只在 `codex/preview` 分支進行，不得自行 merge 到 `main`。
- 手機版優先，但不可忽略桌面版裁切與定位。
- 保留滿版換頁、原有柔和轉場、各頁不同底色與持續微動態。
- 不可把資訊頁改回一般長頁面捲動。
- 修改完成後依 `PROJECT_CONTEXT.md` 的測試清單檢查。
- 新的設計決策、已知問題或素材替換完成後，更新 `PROJECT_CONTEXT.md`。

